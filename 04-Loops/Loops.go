package main

import (
	"fmt"
)

func main() {
	//Laço for para exibir os inteiros de à 9
	for i := 0; i < 10; i++ {
		fmt.Println(i)
	}

	//Laço for para exibir a letra y infinitamente
	for i := 1; i == 0; i++ {
		fmt.Print("y")
	}
}
